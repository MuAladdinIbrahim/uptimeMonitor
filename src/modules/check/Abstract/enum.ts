export enum Protocol {
    HTTP = "HTTP",
    HTTPS = "HTTPS",
    TCP = "TCP",
  }
  
  export enum CheckState {
    RUNNING = "RUNNING",
    PAUSED = "PAUSED",
    DELETED = "DELETED",
  }

  export enum CheckStatus {
    UP = "UP",
    DOWN = "DOWN",
  }